spring:
  application:
    name: userservice
  datasource:
    url: jdbc:postgresql://db:5432/greg
    driverClassName: org.postgresql.Driver
    username: postgres
    password: admin
    jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect

flyway:
  user: postgres
  password: admin
  url: jdbc:postgresql://db:5432/greg

elasticsearch:
  host: es:9200